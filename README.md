# server

## CLI Commands

```bash
# install dependencies
yarn

# build for production with minification
yarn build

# start the production build locally
yarn start

# code linting
yarn lint

# run tests with jest
yarn test
```
